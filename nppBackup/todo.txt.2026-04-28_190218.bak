Claude‑Optimised Diff‑Only Template

Use diff-only output. 
Do NOT rewrite whole files unless I explicitly ask.
Show only the minimal patch with a few lines of context.
No explanations unless I request them.
Keep responses short and focused.


Token‑Efficient Prompting Template for Chronicle

Coding rules for Chronicle:
- Vanilla JS ES modules, no build step.
- State lives in app.js; save via saveData().
- Modals use history API.
- Event/todo titles stored as HTML; use esc() for user content.
- Touch-first interactions; 44px tap targets.
- CSS variables everywhere.
- Always call applyUITheme() + injectEventThemeCSS() together.
- Use diff-only output unless I ask otherwise.
- Keep answers concise by default.


DIFF-ONLY + LOCATION-AWARE MODE:

When modifying code:
- Output ONLY a minimal diff.
- NEVER rewrite entire files unless I explicitly say “full rewrite allowed”.
- ALWAYS include the file name at the top of the diff.
- ALWAYS include the function name or nearest identifiable block.
- ALWAYS include 2–5 lines of context above and below the change.
- NEVER modify unrelated code.
- NEVER restructure or optimise unless I explicitly ask.
- NEVER add commentary unless I request it.
- ALWAYS include line numbers when I provide the snippet or full file.

Format:
1. State the file path (e.g., js/events.js)
2. State the function or block name (e.g., function clearDay(dateKey))
3. Provide a minimal diff with context:
   ```diff
   @@ location info @@
   - old line
   + new line
   
   
DIFF-ONLY + LOCATION-AWARE MODE: new

When modifying code:
- Output ONLY a minimal diff.
- NEVER rewrite entire files unless I explicitly say "full rewrite allowed".
- NEVER refactor, rename, reorder, or restructure code unless explicitly requested.
- NEVER modify unrelated code.

Context rules:
- ALWAYS include the file name at the top.
- ALWAYS include the function name or nearest identifiable block.
- ALWAYS include 2–5 lines of REAL context above and below the change.
- NEVER invent or guess missing code.
- If context is insufficient → ASK for more code instead of guessing.

Line numbers:
- If I provide line numbers → you MUST use them.
- If I do NOT provide line numbers → DO NOT invent them.

Diff format (STRICT):
1. File path
2. Function/block name
3. Diff:

```diff
@@ exact location (line numbers if available) @@
  context line
- old code
+ new code
  context line


DEBUG MODE (ROOT-CAUSE FIRST):

When analysing code issues:

- DO NOT provide a fix immediately.
- FIRST identify the exact root cause.
- Be precise: point to the exact line(s) or pattern causing the issue.
- Explain WHY the error occurs (scope, timing, DOM state, event flow, etc).

Structure your response in this order:

1. ROOT CAUSE
- Exact reason the error happens
- Reference specific lines or variables

2. WHAT IS BROKEN
- What behaviour fails (not just the error message)

3. WHY IT HAPPENS IN THIS CODEBASE
- Tie it to my architecture (event listeners, init order, DOM creation, etc)

4. MINIMAL FIX OPTIONS
- Provide 1–2 small fixes only
- No rewrites
- No refactors

Rules:
- DO NOT rewrite full files
- DO NOT suggest large structural changes
- DO NOT assume missing code — ask if needed
- KEEP explanations tight and technical

If the issue is caused by multiple interacting bugs, list them separately.

----------------------------------------


SMART CODE MODE (DEBUG ↔ DIFF ↔ INTERACTIVE FIX)
Claude must choose the correct mode automatically.

MODE SELECTION
Use DEBUG MODE if:

Errors, bugs, or unclear behaviour are mentioned

I ask “why”, “what’s wrong”, or “explain”

Use DIFF MODE if:

I explicitly request a fix/change AND it is clearly defined

Use INTERACTIVE FIX MODE if:

Multiple possible fixes exist

OR the fix could impact behaviour

OR uncertainty exists

Default → DEBUG MODE

DEBUG MODE (ROOT‑CAUSE FIRST)
When in DEBUG MODE, Claude MUST NOT output code or diffs.

ROOT CAUSE

Exact reason

Point to specific code

WHAT IS BROKEN

WHY IN THIS CODEBASE

FIX OPTIONS (NUMBERED)  
Each fix must include:

What it changes

Risk level (Low / Medium / High)

Scope (lines/functions affected)

End with:
“Reply with the fix number(s) to apply, or say ‘apply safest’.”

DEBUG MODE RULE:

No code, no diffs, no patches.

INTERACTIVE FIX MODE
Triggered when I reply with:

A number → apply ONLY that fix

Multiple numbers → apply them in order

“apply safest” → choose lowest‑risk fix

Then switch to DIFF MODE.

DIFF MODE (STRICT, LOCATION‑AWARE)
Claude MUST:

Output ONLY minimal diffs

NEVER rewrite full files

NEVER rewrite full functions

NEVER modify unrelated code

NEVER refactor unless explicitly requested

ALWAYS include:

File path

Function/block name

Line numbers (if snippet provided)

2–5 lines of context

----------------------

We are implementing Google Drive backup. This is a large feature.

Full rewrite allowed for this request.

Start in INTERACTIVE FIX MODE:
- Propose the architecture
- List all new files
- List all modified files
- Describe the data flow
- Describe the OAuth flow
- Describe the sync logic
- Describe the UI changes

Do NOT output code yet.

After I confirm, switch to DIFF MODE and generate the patches.


keytool -list -v -keystore "S:\Coding\Chronicle Android\Chronicle\Chronicle\jcatkey.jks" -alias chronicle

Certificate fingerprints:
         SHA1: 4D:7F:6F:E9:3D:5A:A4:06:95:4B:5C:C4:6E:D9:F8:E8:97:51:91:C1
         SHA256: C1:48:54:00:65:19:69:81:98:FF:99:B4:52:CB:63:06:81:95:46:54:E0:F2:62:5C:BC:7F:77:4F:B4:C3:21:78
         
         
if i want to tweak the 50% swipe trigger , what code do i change
In app.js, the initGestures call at the bottom of the file has these two lines:


commitThresholdH: () => window.innerWidth  * 0.5,
commitThresholdV: () => window.innerHeight * 0.5,
Change 0.5 to whatever fraction you want — e.g. 0.35 for 35% of screen width/height. H is the left/right (week) swipe, V is the up/down (month) swipe.

For the expanded day view, the threshold is separately in app.js inside _handleExpEnd:


if (Math.abs(dx) >= window.innerWidth * 0.5) {
Same idea — change 0.5 there to adjust the day-swipe trigger independently.
